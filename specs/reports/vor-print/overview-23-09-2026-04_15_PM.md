# VOR Print — Spec: Overview

**Created:** 23-09-2026 04:15 PM IST

## Status

Implemented (search-parameters-only stage).

## Purpose

Vehicle Off Road (VOR) print report.

## Current state

- `ReportConfig`: `src/app/features/vor-print/vor-print.config.ts` — `id: 'vor-print'`,
  `route: 'vor-print'`, `hasTable: false`, `apiIntegrated: false`.
- Routed (see `app.routes.ts`) to the shared `ReportSearchOnlyPageComponent` — no dedicated
  feature page/store/service of its own yet, per the Multi-Report Framework Specification
  (reports without a confirmed data source get a route and search parameters, never a
  fabricated table).
- Page look now matches every other report: blue `ReportHeaderBarComponent` bar (full page
  width), `ReportDealerIdentityComponent` identity line, a Date Range search field, a themed
  "Show Report"/"Reset" button pair, and a placeholder `DataTableComponent` (generic A/B/C/D
  columns, placeholder rows) standing in for the real table until its schema is confirmed —
  `report-search-only-page-table-and-button-theme-23-09-2026-03_30_PM.md`. No editable Dealer
  Code/Description/Company Code fields — `remove-dealer-code-description-company-code-section-23-09-2026-04_00_PM.md`.

## Open decisions

- Real data source/API contract and table schema are unconfirmed — no store/service exists yet.
  Once confirmed, this report needs its own page component, store, and real column definitions,
  replacing the shared placeholder table.
