/**
 * Base URL for the platform's report API. Plain constant rather than an `environment.ts`
 * file — no environment-based config exists yet in this project (api-integration-16-09-2026-02_28_PM.md).
 */
export const API_BASE_URL = 'http://localhost:8080/api/v1/';
